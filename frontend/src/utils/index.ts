/**
 * @desc プロジェクトで使用するUtils（別プロジェクトに移行したらすぐ使える）
 */

export const square = (num: number) => {
  return num * 2;
};

// const spanWrapText = (target) => {
//   const nodes = [...target.childNodes];

//   let result = '';

//   for (const node of nodes) {
//     if (node.node)
//   }
// }
